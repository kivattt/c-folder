swr_set_target_buffer(buffer, width, height)

in swr_set_target_buffer():
	if (buffer == NULL) printf("you passed NULL, oops");

in functions: CHECK_BUFFER(): if (buffer == NULL) printf("did you forget set_target_buffer?");

swr_draw_image();
swr_draw_image_extra();



SWRender r = swr_initialize();

swr_set_


//
SWRender r;
swr_initialize(&r);
swr_set_buffer(&r, buf, w, h);

swr_draw_text(&r, "hello world!", 0, 0);
swr_draw_text_ex(&r, ...);
