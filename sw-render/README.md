## sw-render.h

A library for software-rendering

## TODOs
Make the parameters consistent, like changing parameter names `buffer, img` to `dest, src` to clarify. \
Remember to change in both .h and .c files...

Add a library prefix like `swr_` or something
